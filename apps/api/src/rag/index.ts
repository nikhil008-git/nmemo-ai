/** @deprecated Use @contextengine/rag-retriever — kept as a thin re-export. */
export {
  ingestPdf,
  search,
  embed,
  RagRetriever,
  COLLECTION,
  ensureCollection,
} from "@contextengine/rag-retriever";
