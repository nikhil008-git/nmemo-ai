# Frontend product docs (internal)

Markdown notes for the **nmemo Context Engine** dashboard.

## App routes (logged in)

| Route | Notes |
|-------|--------|
| `/home` | Hub + setup steps |
| `/playground` | Ask + context inspector |
| `/sources` | PDF ingest |
| `/connectors` | Slack / GitHub / Notion / mem0 / Qdrant |
| `/keys` | API keys + SDK snippet |
| `/settings` | Profile only |
| `/docs/*` | Customer-facing docs |

Legacy redirects: `/dashboard` → `/home`, `/chat` → `/playground`.
