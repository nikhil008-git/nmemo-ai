# Workspace onboarding — planned

1. Sign up / sign in
2. Create or join workspace
3. Connect first source (Qdrant / docs, or mem0)
4. Ingest a document (RAG)
5. Try `/chat` demo powered by `getContext()`
6. Create an API key for `@contextengine/sdk`
