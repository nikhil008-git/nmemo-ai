/**
 * The single stage backdrop — one photograph behind every product shell, the
 * feature cards, the journey panels, and the closing CTA.
 *
 * The value lives in `globals.css` so the dark theme can lay a veil over the
 * same photograph without any component knowing which theme is active.
 */

export const stageBackground = "var(--stage-photo)";
